name: l3s_agent_311

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      # Minimal test dependency plus this package and its declared runtime deps.
      - pytest>=8,<9
      - -e .
